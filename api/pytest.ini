[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: full-length playthroughs; deselect with -m "not slow"
    integration: needs MongoDB and/or MinIO (skips itself when they are down)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
