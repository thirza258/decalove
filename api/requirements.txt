fastapi>=0.104.1
uvicorn[standard]>=0.24.0
motor>=3.3.1
minio>=7.2.0
pydantic>=2.4.2
pydantic-settings>=2.0.3
python-multipart>=0.0.6
python-dotenv>=1.0.0
httpx>=0.27.0
# Background Task Queue (Celery + Redis)
celery>=5.3.0
redis>=5.0.0
# Sprite background removal (app/assets/transparency.py). Declared rather than inherited:
# these used to arrive as diffusers' own dependencies, so moving SDXL to
# requirements-sdxl.txt would otherwise have taken them along and quietly turned every
# generated sprite opaque -- the module falls back to the untouched image on ImportError.
# Needed on the openrouter image path too, which wants no torch at all.
Pillow>=10.0.0
numpy>=1.24.0
